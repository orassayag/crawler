/*     console.log(stats.mtime);
    console.log(diffLastUpdateResult.minutes); */

            //const linksPath = logService.crawlLinksPath;
        //console.log(distPath);
    /*                     crawlInterval: null, */
            /*         if (data.crawlInterval) {
                    clearInterval(data.crawlInterval);
                } */
    //        this.applicationData.isProductionMode, this.applicationData.maximumMinutesWithoutUpdate
    /*     close() {
            if (this.monitorInterval) {
                clearInterval(this.monitorInterval);
            }
        } */

    //const systemUtils = require('./system.utils');
    //const { Color } = require('../../core/enums/files/text.enum');
    /*             console.log(minutes);
                console.log(maximumMinutesWithoutUpdate); */
    //endProcess
    //        this.lastUpdateLogFiles = null;
    //            //this.lastUpdateLogFiles = stats.mtime;
    /*             const stats = fs.statSync(distPath);
console.log(stats.mtime);
console.log('\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n\n');
process.stdout.cursorTo(0); */
    //console.log(distPath);
    /*                 startDateTime: this.lastUpdateLogFiles,
            endDateTime: new Date() */
    /* const { Color } = require('../../core/enums'); */
    //const { Color } = require('../../core/enums/files/text.enum');
    /*                 await endProcess({
        crawlInterval: null,
        exitReason: 'APPLICATION STUCK. RESTARTING',
        color: Color.YELLOW,
        code: 1
    });
    this.close(); */

    /*     initiateLogFilesMonitor() {
            globalUtils.monitorLogFiles(this.applicationData.isProductionMode, this.applicationData.maximumMinutesWithoutUpdate);
        } */

                        /*                 crawlInterval: crawlInterval, */
/*                 crawlInterval: crawlInterval, */
            /* crawlInterval,  */
/*             crawlInterval: crawlInterval, */